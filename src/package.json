{
  "name": "src",
  "version": "1.0.0",
  "main": "api/server.js",
  "scripts": {
    "start-local": "json-server -p 3000 --watch db.json --routes routes.json",
    "start-node": "node api/server.js"
  },
  "private": true,
  "dependencies": {
    "json-server": "0.17.4"
  }
}
